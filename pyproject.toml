[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.coverage.run]
source = ["app"]
omit = ["app/__init__.py", "app/routers/__init__.py"]

[tool.coverage.report]
fail_under = 90
show_missing = true
